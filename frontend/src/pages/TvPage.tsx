import { TvDisplay } from '../tv/TvDisplay';

// /tv — a NubArca display in a browser. Everything is in TvDisplay: pairing,
// the control plane, the assigned party and the general experience.
export function TvPage() {
  return <TvDisplay />;
}
