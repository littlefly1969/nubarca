# Generated by running the suite — never committed.
node_modules/
playwright-report/
test-results/
.state/
.artifacts/

# Playwright browser engines live in the user cache, but guard against a local
# PLAYWRIGHT_BROWSERS_PATH=0 putting them here.
.playwright/
ms-playwright/
