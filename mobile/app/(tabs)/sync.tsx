// Sync tab: the device→NubArca synchronization surface.
import { SyncScreen } from '../../src/sync/SyncScreen';

export default function SyncTabPage(): React.JSX.Element {
  return <SyncScreen />;
}
